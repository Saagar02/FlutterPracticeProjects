import 'package:flutter/material.dart';

class expertisePage extends StatelessWidget{

  const expertisePage({Key? key});
  @override
  Widget build(BuildContext context) {
  
    return Container(
      
      color: Colors.orange,
    );
  }
}